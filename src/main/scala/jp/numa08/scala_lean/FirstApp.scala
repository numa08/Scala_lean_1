package jp.numa08.scala_lean

import scala.util.Random
import jp.numa08.models._

object FirstApp extends App {
	println("hello world")


	// val wasyoku = new Shop("ohtoya",20)

	// val weight:Int = wasyoku.weight
	// println(weight)
	// val str:String = wasyoku.toStr(",")
	// println(str)

	// val shops:List[Shop] = new Shop("ohtoya", 10) :: new Shop("mac", 40) :: new Shop("yoshinoya", 60) :: Nil

	// val chooser:Chooser = new Chooser

	// val d = ()=> {
	// 	val rand = new Random
	// 	(rand.nextInt(50) + 1)  + (rand.nextInt(50) + 1)
	// }
	
	// val shop:Shop = chooser.choose(shops, d)
	// println(shop.name)
}